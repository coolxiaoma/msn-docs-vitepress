{
  "devDependencies": {
    "vitepress": "^1.6.4",
    "rimraf": "^6.0.1",
    "cpr": "^3.0.1"
  },
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build && npx rimraf docs && npx cpr .vitepress/dist docs -o",
    "preview": "vitepress preview"
  }
}